<%= if is_admin?(@current_user) do %>
<h2>Edit Service</h2>

<%= render "form.html", Map.put(assigns, :action, service_path(@conn, :update, @service)) %>

<span><%= link "Back", to: service_path(@conn, :index) %></span>
<% else %>
<p>Contact an admin to view this page.</p>
<% end %>
