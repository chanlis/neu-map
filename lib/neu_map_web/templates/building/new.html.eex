<%= if is_admin?(@current_user) do %>
<h2>New Building</h2>

<%= render "form.html", Map.put(assigns, :action,  building_path(@conn, :create)) %>

<span><%= link "Back", to: building_path(@conn, :index) %></span>
<% else %>
<p>Contact an admin to view this page.</p>
<% end %>
